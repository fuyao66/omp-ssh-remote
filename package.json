{
  "name": "omp-ssh-remote",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Native remote OMP workspace tools over a persistent SSH runtime",
  "license": "MIT",
  "files": [
    "dist",
    "README.md",
    "README.zh-CN.md"
  ],
  "scripts": {
    "build": "bun scripts/build.ts",
    "build:worker:arm64": "bun scripts/compile-worker.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun run build && bun test"
  },
  "dependencies": {
    "@oh-my-pi/pi-coding-agent": "17.3.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.0"
  },
  "omp": {
    "extensions": [
      "./dist/extension.js"
    ]
  },
  "pi": {
    "extensions": [
      "./dist/extension.js"
    ]
  }
}
